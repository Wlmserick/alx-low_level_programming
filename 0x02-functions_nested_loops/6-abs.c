#include "main.h"
/**
* _abs- computes the absolute value of a number
* @r: is the int to be checked
*Return: absolute value of int.
*/
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
